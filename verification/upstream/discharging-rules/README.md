# Discharging Rules
The directory `R` contains rule files in $\mathcal{R}$.
There are 43 rules in $\mathcal{R}$.
Two rules of them are symmetry, so considering all mirrors, there are 84 rules.
Thus, there are 84 rules in the `R` directory.


